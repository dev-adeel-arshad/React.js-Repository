import InputBox from "./InputBox";

export{InputBox};

// this is better way to export your components means first import all the files in one folder like index.js and then export all these files from this single folder